[package]
name = "engine-env"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib"]

[dependencies]
abi = {path = "../abi"}
boxgraph = {path = "../boxgraph"}
value = {path = "../value"}
math = {path = "../math"}
dsp = {path = "../dsp"}
