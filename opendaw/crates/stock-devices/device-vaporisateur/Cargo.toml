[package]
name = "device-vaporisateur"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
abi = {path = "../../abi"}
math = {path = "../../math"}
dsp = {path = "../../dsp"}
voicing = {path = "../../voicing"}
libm = "0.2"
