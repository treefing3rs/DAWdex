[package]
name = "signalsmith-wasmbench"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
signalsmith = { path = "../signalsmith" }

[profile.release]
opt-level = 3
lto = true
panic = "abort"
