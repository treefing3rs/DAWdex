# Device: FX Composite

Run several effect chains in parallel and blend them together.

Highlights, in order:
- The idea: layer effects side by side instead of one after another.
- Classic use: parallel compression — a clean layer plus a heavily processed one, mixed with dry/wet.
